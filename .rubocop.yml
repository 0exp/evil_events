inherit_gem:
  armitage-rubocop:
    - lib/rubocop.general.yml
    - lib/rubocop.rspec.yml

AllCops:
  UseCache: true
  TargetRubyVersion: 2.4.5
  Include:
    - bin/console
    - bin/rspec
    - lib/**/*.rb
    - spec/**/*.rb
    - Gemfile
    - Rakefile
    - evil_events.gemspec
