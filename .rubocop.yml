inherit_gem:
  armitage-rubocop:
    - lib/rubocop.general.yml
    - lib/rubocop.rspec.yml

AllCops:
  UseCache: true
  TargetRubyVersion: 2.4.4
  Include:
    - bin/console
    - bin/rspec
    - lib/**/*
    - spec/**/*
    - Gemfile
    - Rakefile
    - evil_events.gemspec
